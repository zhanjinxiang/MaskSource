# 可运行性验证记录

验证环境：Linux x86_64、Python 3.12，全新虚拟环境按`requirements.txt`安装。

## 自动测试

```text
14 passed
```

覆盖内容：

- 提示与图像双密钥同时匹配时验证成功；
- 只有单一触发器时拒绝激活；
- 模型文件被修改后验证失败；
- 模型内嵌记录与外部审计清单一致，清单篡改后验证失败；
- YAML配置被真实读取，非法配置会被拒绝；
- 96位文本版权载荷完整嵌入与恢复；
- 文本发生局部插入、删除和替换后仍可重同步验证；
- 错误身份无法通过文本水印验证；
- 48位DWT图像水印完整嵌入与恢复；
- JPEG质量60压缩后图像水印仍可验证；
- Flask健康检查、端到端演示、嵌入/验证和证据导出接口。

## 本次合成演示实测

```text
模型双密钥验证：verified = true, confidence = 1.0
文本水印验证：verified = true, BER = 0.0
图像水印验证：verified = true, BER = 0.0
Web健康检查：status = ok
```

具体容量、PSNR和SSIM以每次生成的`demo_report.json`为准。以上结果来自程序自动生成的匿名合成资产，只用于验证当前代码路径能够正确执行，不替代作品报告中的大模型实验。
