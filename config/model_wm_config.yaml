# 模型版权保护子系统（可运行参考后端）
model:
  name: "local-model-artifact"
  device: "cpu"

trigger:
  image_secret: "data/secret_image.png"
  image_hash: "perceptual-plus-colour"
  image_tolerance: 0.12
  prompt_hash: "hmac-sha256"

verification:
  require_prompt_key: true
  require_image_key: true
  require_model_digest: true
